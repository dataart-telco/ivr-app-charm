
---
EXTERNAL_IP_VAR: "{{ EXTERNAL_IP_VAR | default("") }}"
RESTCOMM_SERVICE_VAR: "{{ RESTCOMM_SERVICE_VAR | default("") }}"
UNIT_NAME_VAR: "{{ UNIT_NAME_VAR | default("") }}"
RESTCOMM_USER_VAR: "{{ RESTCOMM_USER_VAR | default("") }}"
RESTCOMM_PASSWORD_VAR: "{{ RESTCOMM_PASSWORD_VAR | default("") }}"
PHONE_NUMBER_VAR: "{{ PHONE_NUMBER_VAR | default("") }}"
DID_DOMAIN_VAR: "{{ DID_DOMAIN_VAR | default("") }}"
